# .readthedocs.yaml
version: 2

sphinx:
  configuration: docs/source/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"   # 避免 grpcio 编译失败！

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .